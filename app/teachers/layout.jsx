export const metadata = {
  title: 'Our German Language Faculty',
  description: 'Meet the certified German language instructors at German Learning School, teaching live Zoom classes from A1 to B2 with a focus on Goethe, telc and ÖSD exam success.',
  alternates: {
    canonical: '/teachers',
  },
};

export default function Layout({ children }) {
  return <>{children}</>;
}
